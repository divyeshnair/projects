*The folder comprises of a file 'config.py' which is a raspberry pi simulator coded in python language.

*The software was designed to configure the date,time,and the necessary parameters through a network when the
 pi and pc both are connected and fall in same network.The software configures the pi by calling the pi's hostname.
 

*Requirements
->Python 2.7.9
->pip install Image